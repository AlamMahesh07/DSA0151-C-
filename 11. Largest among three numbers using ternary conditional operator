#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cout << "Enter three integers: ";
    cin >> a >> b >> c;
    cout << "The largest number is: " << ((a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c)) << endl;
    return 0;
}
